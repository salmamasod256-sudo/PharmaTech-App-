/* هذا هو ملف CSS الذي أرسلته، لم يطرأ عليه تغيير */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Amiri', serif;
}

body, html {
    height: 100%;
    /* تأكد من أن رابط الصورة صحيح وفعال عند الرفع */
    background: url('https://i.postimg.cc/d7Jty8s1/IMG-20251016-WA0017.jpg') no-repeat center center/cover; 
    background-attachment: fixed;
    position: relative;
    direction: rtl;
}

/* ---------------------------------------------------- */
/* الرسوم المتحركة                                     */
/* ---------------------------------------------------- */
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

/* ---------------------------------------------------- */
/* تنسيقات واجهة المسح (Scanner Interface)              */
/* ---------------------------------------------------- */
.scanner-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.95);
    z-index: 200;
    display: none; 
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: white;
    padding: 20px;
    text-align: center;
}
.scanner-content {
    width: 90%;
    max-width: 400px;
    text-align: center;
}
#qr-reader-content { /* تم تعديل التنسيق هنا للعنصر الجديد */
    width: 100%;
    min-height: 250px; /* لضمان ظهور واجهة الكاميرا */
    margin-bottom: 20px;
    background: #222;
    border-radius: 10px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    border: 2px dashed #a0522d;
    font-size: 1.2em;
    padding: 20px;
    color: #fff;
}
.scanner-btn {
    padding: 10px 20px;
    background: #a0522d;
    border: none;
    border-radius: 10px;
    color: white;
    cursor: pointer;
    font-size: 1.1em;
    margin-top: 15px;
    width: 100%;
    max-width: 300px;
    transition: 0.3s;
}
.scanner-btn:hover {
    opacity: 0.8;
}

/* ---------------------------------------------------- */
/* تنسيقات الواجهة الرئيسية (index.html)                 */
/* ---------------------------------------------------- */
#page-index {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
    overflow: hidden;
}
#page-index h1 {
    font-size: 2.3em;
    margin-bottom: 50px;
    font-weight: bold;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
    opacity: 0;
    animation: fadeIn 1s ease-out forwards;
    animation-delay: 0.5s;
}
#page-index h1 span {
    font-family: 'Times New Roman', Times, serif;
    letter-spacing: 1px;
    color: white;
    text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.9);
}
#page-index .buttons {
    display: flex;
    flex-direction: column;
    gap: 20px;
    align-items: center;
    opacity: 0;
    animation: fadeIn 1s ease-out forwards;
    animation-delay: 1.5s;
}
#page-index button {
    width: 250px;
    padding: 12px;
    border: none;
    border-radius: 25px;
    font-size: 1.2em;
    font-weight: bold;
    cursor: pointer;
    transition: 0.3s;
    color: #fff;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
}
#page-index button:first-child {
    background: #a0522d;
}
#page-index button.chat {
    background: rgba(255, 218, 185, 0.3);
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.4);
}
#page-index button:hover {
    transform: scale(1.05);
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.4);
}

/* ---------------------------------------------------- */
/* تنسيقات صفحة الإدخال (page2.html)                     */
/* ---------------------------------------------------- */
#page-form {
    min-height: 100vh;
    display: none; 
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 40px 20px 80px 20px;
    color: white;
    position: relative;
}
#page-form h2 {
    font-size: 2em;
    margin-bottom: 30px;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.9);
}
#page-form form {
    display: flex;
    flex-direction: column;
    gap: 15px;
    width: 100%;
    max-width: 400px;
    position: relative;
    z-index: 2;
}
#page-form label {
    text-align: right;
    font-size: 1.1em;
    font-weight: bold;
    color: white;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.7);
}
#page-form input, #page-form select {
    padding: 12px 15px;
    border: 1px solid rgba(255, 255, 255, 0.4);
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.2);
    color: white;
    font-size: 1em;
    outline: none;
    transition: 0.3s;
}
#page-form select option {
    background: white;
    color: black;
}
#page-form button.submit {
    margin-top: 25px;
    padding: 12px;
    border: none;
    border-radius: 15px;
    background: #a0522d;
    color: white;
    font-weight: bold;
    font-size: 1.2em;
    cursor: pointer;
    align-self: center;
    width: 150px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
}

/* زر الكاميرا */
.camera-btn {
    position: fixed;
    bottom: 20px;
    right: 20px;
    width: 50px;
    height: 50px;
    background: rgba(255, 255, 255, 0.2);
    border: 1px solid rgba(255, 255, 255, 0.4);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.4);
    transition: 0.3s;
    z-index: 10;
}
.camera-icon { fill: white; width: 28px; height: 28px; }

/* ---------------------------------------------------- */
/* تنسيقات صفحة النتيجة (page3.html)                      */
/* ---------------------------------------------------- */
#page-result {
    min-height: 100vh;
    display: none;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    padding-top: 20px;
}
.result-card {
    position: relative;
    width: 90%;
    max-width: 400px;
    padding: 25px;
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
    color: white;
    z-index: 10;
    text-align: right;
    margin-bottom: 90px;
    max-height: 85vh;
    overflow-y: auto;
}
.welcome-header { font-size: 1.6em; margin-bottom: 20px; text-align: center; font-weight: bold; text-shadow: 2px 2px 4px #000; }
.drug-name { font-size: 2em; font-weight: bold; text-align: center; margin-bottom: 25px; border-bottom: 1px solid rgba(255, 255, 255, 0.5); padding-bottom: 10px; text-shadow: 2px 2px 5px #000; }
.result-card ul { list-style-type: none; padding: 0; margin: 0; }
.result-card li { margin-bottom: 18px; font-size: 1.2em; line-height: 1.6; text-shadow: 1px 1px 2px #000; }
.result-card li div { margin-top: 5px; font-size: 1em; opacity: 0.9; padding-right: 15px; }
.warning { color: #ffd700; text-align: center; font-size: 1.4em; font-weight: bold; margin-top: 20px; margin-bottom: 10px; text-shadow: 1px 1px 2px #000; }
.restriction { font-size: 1.1em; padding-right: 15px; text-shadow: 1px 1px 2px #000; }

/* أزرار الأسفل */
.action-buttons {
    position: fixed; bottom: 0; left: 0; right: 0; display: flex; justify-content: space-around;
    align-items: center; padding: 10px 5px; background: rgba(0, 0, 0, 0.6); backdrop-filter: blur(5px); z-index: 100;
}
.action-btn {
    display: flex; flex-direction: column; align-items: center; gap: 3px;
    color: white; font-size: 0.7em; font-weight: bold; text-align: center;
    cursor: pointer; background: transparent; border: none; padding: 5px; transition: transform 0.2s;
}
.action-btn:hover { transform: scale(1.05); color: #ffd700; }
.action-icon { width: 24px; height: 24px; fill: white; }
.action-btn:hover .action-icon { fill: #ffd700; }
